import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CardVault AI — Pokémon TCG Portfolio Management",
  description: "Track inventory, monitor prices, calculate taxes, and manage your Pokémon TCG collection like a financial asset.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full`}>
      <body className="min-h-full" style={{ background: "var(--vault-bg)", color: "var(--vault-text)" }}>
        {children}
      </body>
    </html>
  );
}
